# Optional: public base URL for frame JPEGs (Cloudflare R2 r2.dev or custom domain).
# Leave unset to load from Vite public/ (e.g. public/bikers-frames/).
# No trailing slash.
# VITE_FRAMES_CDN_BASE=https://pub-xxxxxxxxxxxx.r2.dev
